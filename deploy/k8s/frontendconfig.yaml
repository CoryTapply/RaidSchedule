apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: raidschedule-frontend-config
  namespace: raidschedule
spec:
  redirectToHttps:
    enabled: true
    responseCodeName: MOVED_PERMANENTLY_DEFAULT
