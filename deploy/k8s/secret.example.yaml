# Reference only — do not apply this file directly and never commit a
# filled-in copy. Create the real secret imperatively instead:
#
#   kubectl create secret generic raidschedule-secrets -n raidschedule \
#     --from-literal=APP_PASSWORD='...' \
#     --from-literal=SESSION_SECRET='...' \
#     --from-literal=RAID_HELPER_API_KEY='...'
apiVersion: v1
kind: Secret
metadata:
  name: raidschedule-secrets
  namespace: raidschedule
type: Opaque
stringData:
  APP_PASSWORD: change-me
  SESSION_SECRET: change-me-change-me-change-me-change-me
  RAID_HELPER_API_KEY: ''
