Naming an icon-only control. Never for information the user actually needs.

```jsx
<Tooltip content="Copy last week across">
  <IconButton label="Copy week">⋯</IconButton>
</Tooltip>
```

Assumes a 38px trigger (offsets are fixed at 44px). Keep it to one line — anything longer is a Popover.
