Notes, descriptions, anything longer than a line.

```jsx
<Field label="Notes" hint="Only officers see this.">
  <Textarea rows={4} placeholder="Bring flasks." />
</Field>
```

Resizes vertically only. Do not set a fixed height — the 84px minimum plus `rows` is enough.
