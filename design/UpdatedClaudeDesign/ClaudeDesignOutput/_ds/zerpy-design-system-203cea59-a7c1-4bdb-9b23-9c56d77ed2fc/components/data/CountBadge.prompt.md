A count next to a label — items scheduled, alts pending, unread.

```jsx
<CountBadge>12</CountBadge>
<CountBadge tone="danger">3</CountBadge>
```

Tabular figures, so a column of counts lines up. Cap the display at "99+" rather than letting the pill stretch.
